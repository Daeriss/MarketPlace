{% extends 'headerShopKeeper.html.twig' %}

{% block title %}Shop index{% endblock %}

{% block body %}
    <h1>Ma boutique</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Arrondissement</th>
                <th>Photo</th>
                <th>Paiement Accepté</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% if (shop)  %}
            <tr>
                <td>{{ shop.name }}</td>
                <td>{{ shop.adress }}</td>
                <td>{{ shop.img }}</td>
                <td>{{shop.paiement}}</td>
                <td>
                    <a href="{{ path('shop_edit', {'id': shop.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endif %}
        
        </tbody>
    </table>

{% endblock %}
