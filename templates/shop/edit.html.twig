{% extends 'base.html.twig' %}

{% block title %}Edit Shop{% endblock %}

{% block body %}
    <h1>Edit Shop</h1>
<div class="container">
    {{ include('shop/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('shop_index') }}">back to list</a>

    {{ include('shop/_delete_form.html.twig') }}
</div>
{% endblock %}
